<h1>Editing Genre</h1>

<%= render 'form', genre: @genre %>

<%= link_to 'Back', genres_path, :class => "btn btn-sm btn-secondary" %>
<%= link_to 'Show', @genre, :class => "btn btn-sm btn-primary" %>
